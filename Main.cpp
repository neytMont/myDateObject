//Nathaniel Monte de Ramos
//CECS 282-07
//Program 2 - myDate Object
//Oct 2, 2019
//#include "myDate.h"

/*int main() {
	int m1 = 10, d1 = 06, y1 = 1996, m2 = 02, d2 = 14, y2 = 2019;
	myDate date1(m1,d1,y1), date2(m2,d2,y2);
	int Greg2Julian(int month, int day, int year);
	void Julian2Greg(int JN, int& month, int& day, int& year);

	date1.display();
	cout << "\nDay: " << date1.getDay() << endl;
	cout << "Month: " << date1.getMonth() << endl;
	cout << "Year: " << date1.getYear() << endl;
	int jDate1 = Greg2Julian(m1, d1, y1);
	cout << "The Julian conversion of the date is " << jDate1 << endl;
	Julian2Greg(jDate1, m1, d1, y1);
	cout << "The Gregorian conversion from the Julian date is " << m1 << "/" << d1 << "/" << y1 << endl << endl;
	
	date2.display();
	cout << "\nDay: " << date2.getDay() << endl;
	cout << "Month: " << date2.getMonth() << endl;
	cout << "Year: " << date2.getYear() << endl;
	int jDate2 = Greg2Julian(m2, d2, y2);
	cout << "The Julian conversion of the date is " << jDate2 << endl;
	Julian2Greg(jDate2, m2, d2, y2);
	cout << "The Gregorian conversion from the Julian date is " << m2 << "/" << d2 << "/" << y2 << endl << endl;

	cout << "\nDays between the two dates: " << date1.daysBetween(date2) << endl;

	system("pause");
	return 0;
}
*/